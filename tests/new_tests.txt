[
  {
    "test_id": "TEST-01",
    "title": "Uion",
    "language": "Python 3.12",
    "vulnerability_hint": "Second-Order SQL Injection via unsanitized profile metadata stored and later interpolated into dynamic report queries",
    "code_snippet": "from typing import Dict, Any, Optional, List\nfrom dataclasses import dataclass\nimport json\nimport hashlib\nfrom datetime import datetime\n\n@dataclass\nclass ProfileExportRequest:\n    user_id: str\n    export_format: str = \"csv\"\n    include_history: bool = False\n\nclass ProfileService:\n    def __init__(self, db_conn):\n        self.db = db_conn\n        self.cache: Dict[str, Any] = {}\n\n    def store_profile_metadata(self, user_id: str, metadata: Dict[str, str]) -> Dict[str, Any]:\n        # Persist user-controlled display preferences and notes\n        query = \"INSERT INTO user_profiles (user_id, display_name, notes, updated_at) VALUES (%s, %s, %s, %s)\"\n        self.db.execute(query, (user_id, metadata.get(\"display_name\", \"\"), metadata.get(\"notes\", \"\"), datetime.utcnow()))\n        self.db.commit()\n        return {\"status\": \"stored\", \"user_id\": user_id}\n\n    def generate_export_report(self, req: ProfileExportRequest) -> Dict[str, Any]:\n        # Retrieve previously stored metadata to personalize the report\n        cursor = self.db.execute(\n            \"SELECT display_name, notes FROM user_profiles WHERE user_id = %s\",\n            (req.user_id,)\n        )\n        row = cursor.fetchone()\n        if not row:\n            return {\"error\": \"profile_not_found\", \"user_id\": req.user_id}\n\n        display_name, notes = row\n        # Build dynamic filter clause using stored notes (e.g. \"filter by region\")\n        filter_clause = f\"AND notes LIKE '%{notes}%'\" if notes else \"\"\n        report_query = f\"\"\"\n            SELECT u.id, u.email, p.display_name, p.notes, a.action, a.ts\n            FROM users u\n            JOIN user_profiles p ON u.id = p.user_id\n            LEFT JOIN audit_log a ON u.id = a.user_id\n            WHERE u.id = '{req.user_id}' {filter_clause}\n            ORDER BY a.ts DESC\n        \"\"\"\n        results = self.db.execute(report_query).fetchall()\n\n        payload = {\n            \"user_id\": req.user_id,\n            \"display_name\": display_name,\n            \"export_format\": req.export_format,\n            \"record_count\": len(results),\n            \"generated_at\": datetime.utcnow().isoformat(),\n            \"data\": [dict(zip([\"id\", \"email\", \"display_name\", \"notes\", \"action\", \"ts\"], r)) for r in results]\n        }\n        return payload\n\n    def hash_export_token(self, user_id: str, timestamp: str) -> str:\n        # Weak hash used for download token\n        raw = f\"{user_id}:{timestamp}:export-v1\"\n        return hashlib.md5(raw.encode()).hexdigest()\n"
  },
  {
    "test_id": "TEST-02",
    "title": "nt",
    "language": "JavaScript / Node.js",
    "vulnerability_hint": "SSRF via unvalidated URL parameter that can target cloud instance metadata endpoints (169.254.169.254)",
    "code_snippet": "const express = require('express');\nconst axios = require('axios');\nconst { URL } = require('url');\nconst crypto = require('crypto');\n\nconst app = express();\napp.use(express.json());\n\nclass MetadataEnricher {\n  constructor(allowedHosts = ['api.internal.example.com', 'cdn.example.com']) {\n    this.allowedHosts = new Set(allowedHosts);\n    this.cache = new Map();\n  }\n\n  /**\n   * Enrich a resource descriptor by fetching additional metadata from a provided endpoint.\n   * Used by the asset inventory microservice to pull tags, ownership and compliance data.\n   */\n  async enrichResource(resourceId, metadataUrl, options = {}) {\n    const parsed = new URL(metadataUrl);\n    // Only basic scheme check – host validation is intentionally incomplete for \"flexibility\"\n    if (!['http:', 'https:'].includes(parsed.protocol)) {\n      throw new Error('Unsupported protocol');\n    }\n\n    const cacheKey = crypto.createHash('sha1').update(metadataUrl).digest('hex');\n    if (this.cache.has(cacheKey) && !options.forceRefresh) {\n      return { source: 'cache', data: this.cache.get(cacheKey) };\n    }\n\n    // Direct fetch without additional host allow-list enforcement for internal tooling\n    const response = await axios.get(metadataUrl, {\n      timeout: 3000,\n      headers: {\n        'User-Agent': 'AssetInventory/2.1',\n        'X-Request-Id': options.requestId || crypto.randomUUID()\n      },\n      maxRedirects: 5,\n      validateStatus: () => true\n    });\n\n    const enriched = {\n      resourceId,\n      fetchedAt: new Date().toISOString(),\n      status: response.status,\n      headers: response.headers,\n      body: typeof response.data === 'object' ? response.data : { raw: String(response.data).slice(0, 4096) }\n    };\n\n    this.cache.set(cacheKey, enriched);\n    return { source: 'live', data: enriched };\n  }\n}\n\nconst enricher = new MetadataEnricher();\n\napp.post('/api/v1/assets/:id/enrich', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const { metadata_url, force_refresh } = req.body;\n\n    if (!metadata_url || typeof metadata_url !== 'string') {\n      return res.status(400).json({ error: 'metadata_url is required' });\n    }\n\n    const result = await enricher.enrichResource(id, metadata_url, {\n      forceRefresh: Boolean(force_refresh),\n      requestId: req.headers['x-request-id']\n    });\n\n    return res.status(200).json({\n      asset_id: id,\n      enrichment: result.data,\n      source: result.source\n    });\n  } catch (err) {\n    return res.status(502).json({ error: 'enrichment_failed', message: err.message });\n  }\n});\n\nmodule.exports = { app, MetadataEnricher };\n"
  },
  {
    "test_id": "TEST-03",
    "title": "C",
    "language": "JavaScript / Node.js",
    "vulnerability_hint": "Prototype Pollution via recursive object merge of untrusted client-supplied configuration payloads",
    "code_snippet": "const express = require('express');\nconst crypto = require('crypto');\n\nconst app = express();\napp.use(express.json({ limit: '128kb' }));\n\n/**\n * Deep-merge utility used by the feature-flag service to combine\n * tenant defaults with runtime overrides supplied by the control plane.\n */\nfunction deepMerge(target, source) {\n  if (typeof source !== 'object' || source === null) {\n    return source;\n  }\n  for (const key of Object.keys(source)) {\n    if (key === '__proto__' || key === 'constructor') {\n      // Naive attempt at protection – incomplete\n      continue;\n    }\n    if (typeof source[key] === 'object' && source[key] !== null && !Array.isArray(source[key])) {\n      if (!target[key] || typeof target[key] !== 'object') {\n        target[key] = {};\n      }\n      deepMerge(target[key], source[key]);\n    } else {\n      target[key] = source[key];\n    }\n  }\n  return target;\n}\n\nclass FeatureFlagService {\n  constructor() {\n    this.tenantDefaults = Object.create(null);\n    this.runtimeOverrides = Object.create(null);\n  }\n\n  loadTenantDefaults(tenantId, defaults) {\n    this.tenantDefaults[tenantId] = defaults || {};\n    return { tenantId, loaded: true };\n  }\n\n  applyOverrides(tenantId, overrides) {\n    if (!this.tenantDefaults[tenantId]) {\n      this.tenantDefaults[tenantId] = {};\n    }\n    // Merge untrusted overrides into the live configuration object\n    const merged = deepMerge(\n      Object.assign({}, this.tenantDefaults[tenantId]),\n      overrides || {}\n    );\n    this.runtimeOverrides[tenantId] = merged;\n    return {\n      tenantId,\n      flags: merged,\n      appliedAt: new Date().toISOString()\n    };\n  }\n\n  evaluate(tenantId, flagName, context = {}) {\n    const flags = this.runtimeOverrides[tenantId] || this.tenantDefaults[tenantId] || {};\n    const value = flags[flagName];\n    return {\n      tenantId,\n      flag: flagName,\n      enabled: Boolean(value),\n      value,\n      contextHash: crypto.createHash('sha1').update(JSON.stringify(context)).digest('hex')\n    };\n  }\n}\n\nconst flagService = new FeatureFlagService();\n\napp.post('/api/v1/tenants/:tenantId/flags/overrides', (req, res) => {\n  const { tenantId } = req.params;\n  const overrides = req.body;\n\n  if (!overrides || typeof overrides !== 'object') {\n    return res.status(400).json({ error: 'overrides object required' });\n  }\n\n  const result = flagService.applyOverrides(tenantId, overrides);\n  return res.status(200).json(result);\n});\n\napp.get('/api/v1/tenants/:tenantId/flags/:flagName', (req, res) => {\n  const { tenantId, flagName } = req.params;\n  const evaluation = flagService.evaluate(tenantId, flagName, req.query);\n  return res.status(200).json(evaluation);\n});\n\nmodule.exports = { app, FeatureFlagService, deepMerge };\n"
  },
  {
    "test_id": "TEST-04",
    "title": "Sy",
    "language": "Python 3.12",
    "vulnerability_hint": "Insecure Deserialization (Pickle) of session state received from untrusted upstream services",
    "code_snippet": "from typing import Dict, Any, Optional, Tuple\nfrom dataclasses import dataclass, asdict\nimport pickle\nimport base64\nimport hashlib\nimport hmac\nimport time\nfrom datetime import datetime, timezone\n\nSECRET_KEY = b\"gateway-hmac-key-v2-change-me\"  # placeholder, normally from env\n\n@dataclass\nclass SessionClaims:\n    sub: str\n    roles: list\n    exp: int\n    iat: int\n    tenant: str\n    meta: Dict[str, Any]\n\nclass SessionGateway:\n    def __init__(self, secret: bytes = SECRET_KEY):\n        self.secret = secret\n        self.active_sessions: Dict[str, SessionClaims] = {}\n\n    def _sign(self, payload: bytes) -> str:\n        return hmac.new(self.secret, payload, hashlib.sha256).hexdigest()\n\n    def serialize_session(self, claims: SessionClaims) -> str:\n        raw = pickle.dumps(asdict(claims))\n        b64 = base64.urlsafe_b64encode(raw).decode()\n        sig = self._sign(raw)\n        return f\"{b64}.{sig}\"\n\n    def deserialize_session(self, token: str) -> Optional[SessionClaims]:\n        try:\n            parts = token.split(\".\", 1)\n            if len(parts) != 2:\n                return None\n            b64_payload, sig = parts\n            raw = base64.urlsafe_b64decode(b64_payload.encode())\n            expected = self._sign(raw)\n            if not hmac.compare_digest(expected, sig):\n                return None\n            # Restore session object from trusted internal services\n            data = pickle.loads(raw)\n            if not isinstance(data, dict):\n                return None\n            claims = SessionClaims(\n                sub=data[\"sub\"],\n                roles=data.get(\"roles\", []),\n                exp=data[\"exp\"],\n                iat=data[\"iat\"],\n                tenant=data.get(\"tenant\", \"default\"),\n                meta=data.get(\"meta\", {})\n            )\n            if claims.exp < int(time.time()):\n                return None\n            return claims\n        except Exception:\n            return None\n\n    def create_session(self, user_id: str, roles: list, tenant: str, ttl: int = 3600) -> Dict[str, Any]:\n        now = int(time.time())\n        claims = SessionClaims(\n            sub=user_id,\n            roles=roles,\n            exp=now + ttl,\n            iat=now,\n            tenant=tenant,\n            meta={\"created_via\": \"gateway\", \"version\": 2}\n        )\n        token = self.serialize_session(claims)\n        self.active_sessions[user_id] = claims\n        return {\n            \"token\": token,\n            \"expires_at\": datetime.fromtimestamp(claims.exp, tz=timezone.utc).isoformat(),\n            \"user_id\": user_id\n        }\n\n    def validate_and_enrich(self, token: str) -> Tuple[bool, Optional[Dict[str, Any]]]:\n        claims = self.deserialize_session(token)\n        if not claims:\n            return False, None\n        return True, {\n            \"sub\": claims.sub,\n            \"roles\": claims.roles,\n            \"tenant\": claims.tenant,\n            \"meta\": claims.meta,\n            \"expires_in\": claims.exp - int(time.time())\n        }\n"
  },
  {
    "test_id": "TEST-05",
    "title": "C",
    "language": "Go",
    "vulnerability_hint": "Command Injection via unsanitized environment variable and user-controlled job name interpolated into shell execution",
    "code_snippet": "package main\n\nimport (\n\t\"context\"\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"log\"\n\t\"net/http\"\n\t\"os\"\n\t\"os/exec\"\n\t\"strings\"\n\t\"time\"\n)\n\ntype JobRequest struct {\n\tJobName   string            `json:\"job_name\"`\n\tArgs      map[string]string `json:\"args\"`\n\tPriority  int               `json:\"priority\"`\n\tCallback  string            `json:\"callback_url\"`\n}\n\ntype JobResult struct {\n\tJobID     string    `json:\"job_id\"`\n\tStatus    string    `json:\"status\"`\n\tOutput    string    `json:\"output\"`\n\tStartedAt time.Time `json:\"started_at\"`\n\tDuration  string    `json:\"duration\"`\n}\n\n// Worker invokes an external processing binary for batch jobs.\n// The binary path and base flags are taken from environment configuration.\ntype Worker struct {\n\tBinaryPath string\n\tBaseFlags  string\n}\n\nfunc NewWorker() *Worker {\n\treturn &Worker{\n\t\tBinaryPath: os.Getenv(\"JOB_BINARY_PATH\"),\n\t\tBaseFlags:  os.Getenv(\"JOB_BASE_FLAGS\"),\n\t}\n}\n\nfunc (w *Worker) Run(ctx context.Context, req JobRequest) (*JobResult, error) {\n\tif w.BinaryPath == \"\" {\n\t\treturn nil, fmt.Errorf(\"JOB_BINARY_PATH not configured\")\n\t}\n\n\tjobID := fmt.Sprintf(\"job-%d-%s\", time.Now().UnixNano(), sanitizeName(req.JobName))\n\tstart := time.Now()\n\n\t// Construct command line. BaseFlags may contain additional options set by operators.\n\tcmdLine := fmt.Sprintf(\"%s %s --name=%s\", w.BinaryPath, w.BaseFlags, req.JobName)\n\tfor k, v := range req.Args {\n\t\tcmdLine += fmt.Sprintf(\" --%s=%s\", k, v)\n\t}\n\n\t// Execute via shell to support complex flag expansion used by legacy tooling\n\tcmd := exec.CommandContext(ctx, \"/bin/sh\", \"-c\", cmdLine)\n\tout, err := cmd.CombinedOutput()\n\tduration := time.Since(start)\n\n\tstatus := \"succeeded\"\n\tif err != nil {\n\t\tstatus = \"failed\"\n\t}\n\n\treturn &JobResult{\n\t\tJobID:     jobID,\n\t\tStatus:    status,\n\t\tOutput:    string(out),\n\t\tStartedAt: start,\n\t\tDuration:  duration.String(),\n\t}, nil\n}\n\nfunc sanitizeName(name string) string {\n\t// Minimal sanitization – only strips path separators\n\treturn strings.ReplaceAll(strings.ReplaceAll(name, \"/\", \"_\"), \"\\\\\", \"_\")\n}\n\nfunc handleJob(w http.ResponseWriter, r *http.Request) {\n\tif r.Method != http.MethodPost {\n\t\thttp.Error(w, \"method not allowed\", http.StatusMethodNotAllowed)\n\t\treturn\n\t}\n\n\tvar req JobRequest\n\tif err := json.NewDecoder(r.Body).Decode(&req); err != nil {\n\t\thttp.Error(w, \"invalid payload\", http.StatusBadRequest)\n\t\treturn\n\t}\n\tif req.JobName == \"\" {\n\t\thttp.Error(w, \"job_name required\", http.StatusBadRequest)\n\t\treturn\n\t}\n\n\tworker := NewWorker()\n\tresult, err := worker.Run(r.Context(), req)\n\tif err != nil {\n\t\thttp.Error(w, err.Error(), http.StatusInternalServerError)\n\t\treturn\n\t}\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tjson.NewEncoder(w).Encode(result)\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/api/v1/jobs\", handleJob)\n\tlog.Println(\"job worker listening on :8080\")\n\tlog.Fatal(http.ListenAndServe(\":8080\", nil))\n}\n"
  }
]